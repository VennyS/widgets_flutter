import 'package:flutter/material.dart';

class BlueTheme {
  static final lightTheme = ThemeData(
    colorScheme: const ColorScheme.light(
      primary: Color(0xFF006FFD),
    ),
    buttonTheme: const ButtonThemeData(buttonColor: Color(0xFF006FFD)),
  );
}
